name: IBK Check PR

on:
  issue_comment:
    types: [created]

jobs:
  check-pr:
    if: github.event.comment.body == 'ibk check pr'
    runs-on: macos-14

    steps:
      - name: 🧾 Checkout PR
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.issue.pull_request.head.ref }}
          repository: ${{ github.event.issue.pull_request.head.repo.full_name }}

      - name: 💬 Saludo inicial
        run: |
          comment_body="🎉 ¡Hola! Gracias por tu comentario 'ibk check pr'. Estamos corriendo tus pruebas ahora mismo 🧪, ¡desea suerte! 🍀"
          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            -X POST \
            -d "{\"body\": \"$comment_body\"}" \
            ${{ github.event.issue.comments_url }}

      - name: 🔧 Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'

      - name: 📦 Install dependencies
        run: bundle install

      - name: ✅ Run Fastlane tests for SPM
        run: bundle exec fastlane test

      - name: 💬 Comentar resultado en el PR
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            comment_body="✅ ¡Éxito! Las pruebas pasaron correctamente. Puedes hacer merge con confianza 🚀"
          else
            comment_body="❌ Algo salió mal. Revisa los errores aquí: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          fi

          curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            -X POST \
            -d "{\"body\": \"$comment_body\"}" \
            ${{ github.event.issue.comments_url }}
